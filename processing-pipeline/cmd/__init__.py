"""Package marker for cmd to allow module-style execution.

This file makes the `cmd` directory a Python package so you can run
the consumer as a module (e.g. `python -m cmd.consumer`).
"""

__all__ = ["consumer"]
